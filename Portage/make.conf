# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
CC="clang"
CXX="clang++"
COMMON_FLAGS="-O3 -march=native -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
#LDFLAGS="-Wl,-O3, -Wl,--as-needed -Wl,--sort-common -flto=1"

# NOTE: This stage was built with the bindist USE flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
MAKEOPTS="-j3"
LC_MESSAGES=C.UTF-8
INPUTS="libinput"
VIDEO_CARDS="intel"
USE="base X opengl vulkan alsa vaapi vdpau libnotify gstreamer jpeg png tiff gif  -systemd -elogind nss gtk gtk3 gtk4 qt5 truetype truetype-fonts -gnome qt6 java l10n_en  pipewire sound-server pcre exif udisks mtp samba iosfuse networkmanager bluetooth policykit dbus extra media-plugins/gst-plugins-meta alsa bluetooth ffmpeg flac http mp3 mpeg ogg opus v4l vorbis x264"

